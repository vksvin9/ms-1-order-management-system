notification:
  email:
    enabled: ${NOTIFICATION_EMAIL_ENABLED:true}
    from: ${NOTIFICATION_EMAIL_FROM:no-reply@vin.com}
    to: ${NOTIFICATION_EMAIL_TO:admin@vin.com}

management:
  endpoints:
    web:
      exposure:
        include: health,info