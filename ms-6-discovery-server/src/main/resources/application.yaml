# server:
#   port: 8761

# spring:
#   application:
#     name: ms-6-discovery-server

# eureka:
#   client:
#     register-with-eureka: false
#     fetch-registry: false

#   server:
#     enable-self-preservation: false

spring:
  application:
    name: discovery-server

  config:
    import: optional:configserver:http://localhost:8888